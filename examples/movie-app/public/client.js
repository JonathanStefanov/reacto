// Minimal client JS — chat only. Everything else is server-rendered.
document.addEventListener('DOMContentLoaded', () => {
  const chat = document.getElementById('chat-panel');
  if (!chat) return;

  // Chat UI will be initialized here if needed
  console.log('🎬 CineLog loaded');
});
